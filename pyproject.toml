[project]
name = "google-docs-mcp"
version = "1.0.0"
description = "FastMCP server for Google Docs"
requires-python = ">=3.10"
dependencies = [
    "fastmcp>=3.0,<4",
    "google-auth>=2.29.0",
    "google-auth-oauthlib>=1.2.0",
    "google-auth-httplib2>=0.2.0",
    "google-api-python-client>=2.122.0",
    "python-dotenv>=1.0.0",
]

[project.scripts]
google-docs-mcp = "src.server:mcp.run"

[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"
